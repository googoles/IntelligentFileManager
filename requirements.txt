# Research File Manager MVP - Core Dependencies (No Rust compilation)
# For Windows users having Rust errors, this avoids ChromaDB compilation

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# Database and ORM
sqlalchemy==2.0.23
pydantic==2.5.0

# File system monitoring
watchdog==3.0.0
aiofiles==23.2.1

# Configuration
python-dotenv==1.0.0

# Document processing (pure Python)
PyPDF2==3.0.1
python-docx==1.1.0
Pillow>=9.0.0

# Math and arrays (pre-compiled)
numpy>=1.21.0

# AI/ML (pre-compiled wheels, no Rust needed)
sentence-transformers==2.2.2

# Optional - install manually if needed:
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
# ollama>=0.5.0
# chromadb>=0.4.18 (requires Rust on Windows)
# easyocr>=1.7.0 (requires compilation)